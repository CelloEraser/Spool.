The if and else commands in Spool allow you to test for things.

The syntax of an if statement goes like this.
```
if(conditional)
{
    statements;
}
(optional)else
{
    statements;
}
```
You can place if statements inside if statements or even inside functions.
